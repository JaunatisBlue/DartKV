# Verified in the dartkv conda environment on 2026-08-23.
# The PyTorch wheel supplies the CUDA runtime used by the reference path;
# the host NVIDIA driver must still be compatible with that runtime.
torch==2.7.1
numpy==2.2.6
pytest==8.4.1
transformers==4.53.2
safetensors==0.8.0
accelerate==1.14.0
