# 第四轮阶段记录：修正随机采样 accuracy batch 协议

时间：2026-08-24 18:57（Asia/Shanghai）

## 上一步概述

上一阶段建立 Qwen3-8B 总验收器，并用 batch16 并行运行 GSM8K。进一步对照 Kitty
`accuracy_eval.sh`/CLI 后发现 accuracy 默认 batch 为 1；随机采样时 batching 会改变 RNG
消耗，因此 batch16 不是严格等价的速度设置，不能用于论文准确率验收。

## 本阶段完成内容

1. 停止两条未完成的 batch16 长任务；已有 batch16 repeat0 保留为诊断，不纳入最终结果；
2. 正式准确率输出切换到 `results/kitty_qwen8_paper_b1`；
3. GPU1 重新启动 Kitty-Pro/GSM8K、GPU0 重新启动 FP16/GSM8K，均固定：
   Qwen3-8B、paper sampling、batch1、repeats3、max_new_tokens4096；
4. Qwen3-8B 总验收器新增 accuracy experiment signature gate，要求：model=Qwen-8B、
   backend=kitty-reference、protocol=paper、batch1、limit=None、Table3生成长度4096、
   Table4生成长度32768、至少3次重复；
5. 新增测试证明 batch16 summary 会被拒绝；全量测试 49 passed；
6. README 示例和说明同步改为 Kitty 官方默认 batch1。

## 当前判断

算子与 Figure 5 已验收；accuracy 现在才进入严格的 artifact-default batching 协议。之前
batch16 单轮 93.63% 不再用于 Table 3 最终判断。两张 A100 当前分别运行 FP16 与 Kitty-Pro
batch1，初始显存约 16.6 GiB。

## 下一步工作安排

1. 完成 FP16/Kitty-Pro GSM8K 三轮并运行 Table 3 audit；
2. 继续用两张 A100 并行安排独立 Qwen3-8B cells，所有 accuracy 均保持 batch1；
3. 完成 Table 3 后继续 Figure 4 和 Table 4；
4. 总验收器持续拒绝错误 batch/重复次数/生成长度，直到全部 gate 通过。
