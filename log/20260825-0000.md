# 第四轮阶段记录：八个 Qwen3-8B accuracy 任务持续运行

时间：2026-08-25 00:00（Asia/Shanghai）

## 上一步概述

上一阶段完成 HumanEval sandbox 的第二次 cache 修正，并正式启动 FP16/Kitty-Pro HumanEval。
当前 Qwen3-8B Table 3 的 GSM8K、MATH、GPQA、HumanEval 四任务均已在两张 A100 上按 batch1、
repeats3 运行。

## 当前状态

- GPU0：FP16 四任务；
- GPU1：Kitty-Pro 四任务；
- 每卡显存约 70 GiB、GPU 利用率 99%，没有 OOM；
- HumanEval FP16 已到 11/164；其余任务仍在长 CoT 前段；
- 尚无完整 repeat checkpoint，符合 runner 的原子写盘设计。

## 当前判断

任务没有退出或显存错误。由于 batch1 随机生成且多个 task 共享 GPU，每个 repeat 预计需要
数小时；不能用早期进度或未完成单样本结果推断最终准确率。

## 下一步工作安排

1. 持续监控进程与显存峰值；
2. 任意 repeat 完成后立即读取 summary、验证 seed/batch/数据 hash；
3. 继续 Table 3 所有 Qwen3-8B cells，随后运行总验收器；
4. 仅在 GPU slot 释放后启动 Table 4/Figure 4，保持 batch1 accuracy 协议。
