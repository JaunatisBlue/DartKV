# 第四轮阶段记录：Figure 5 8192 长上下文 batch2 对照

时间：2026-08-24 01:35（Asia/Shanghai）

## 上一步概述

上一周期在总长度 2048 上完成 batch4/8/32/64/128 sweep，发现 Kitty engine 在 batch64
达到 1.20×、batch128 达到 1.78× FA2 dense 吞吐，证明大 batch 下压缩 KV 会带来优势；
同时修正了 engine dense-bytes 漏乘 batch 的统计 bug。本周期将同样的协议移到论文 Figure 5
要求的总长度 8192。

## 本周期完成内容

固定 Qwen3-8B、Kitty `prompt_choice=1`、chat template、FP16、A100、总长度 8192、warmup 1、
repeat 1：

| batch | FA2 dense tok/s | Kitty engine tok/s | Kitty/dense | KV storage ratio |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 21.66 | 16.92 | 0.781× | 5.39× |
| 2 | 41.95 | 34.45 | 0.821× | 5.39× |

batch2 的 Kitty engine 用时 407.16 s、峰值显存 17.70 GB、storage 448.3 MB；FA2 dense 用时
334.39 s、峰值显存 18.84 GB、dense KV 2.416 GB。两者生成 token 前缀一致。长上下文 batch2
仍低于 dense，说明 2048 上观察到的 batch64/128 反超不能直接外推到 8192，必须继续测更大
batch 并关注 OOM/launch 配置。

## 当前判断

FlashAttention 已正确安装且实际参与 dense baseline；Kitty engine 也能完成 8192 长序列。
但论文 Figure 5 的加速比尚未复现：当前 8192 batch1/2 均约 0.8×，与论文 2.1–4.1× 区间
存在明显差距。差距可能在更大 batch 才反转，也可能来自 Triton/PyTorch 版本和 kernel 计时
边界，不能仅凭 batch2 下结论。

## 下一步工作安排

1. 继续跑 8192 的 Kitty engine/FA2 dense `batch=4,8`；每个点保留完整 warmup/repeat、
   OOM 和显存数据，建立 Figure 5 长上下文曲线；
2. 若 batch8 仍低于 dense，使用 PyTorch profiler/Triton kernel 时间拆分，重点检查 page
   attention 每 token launch、quant-pack 和 Python cache update；
3. 逐项对照官方 Kitty benchmark 的计时边界、`disable_compile`、cache 初始化和模型
   attention implementation，确认当前基准没有把固定分配或 warmup 错计入 repeat；
4. 若需要，建立 PyTorch 2.4.1/CUDA 12.1/Triton 对齐环境做 A/B，保留当前环境用于测试；
5. GPU1 的完整 Kitty-Pro/GSM8K 任务结束后运行 Table 3 audit，继续 Figure 4/ Table 4，
   最终以 manifest 验收而不是单点性能结果结束目标。
