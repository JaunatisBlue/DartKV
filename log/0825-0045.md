# 第四轮阶段记录：GSM8K batch16 正式 checkpoint 续跑

时间：2026-08-25 00:45（Asia/Shanghai）

## 当前进度

- FP16/GSM8K repeat0：688/1319 requests；
- Kitty-Pro/GSM8K repeat1：704/1319 requests（repeat0 已有同协议完整结果）；
- 两个进程分别使用 GPU0/GPU1，显存约 46 GiB/卡；本次监控同时记录 `utilization.gpu` 百分数和 `power.draw` 瓦特，未将二者混淆；
- GPU 持续高利用率，checkpoint 文件按 batch16 边界原子更新，无 OOM、异常退出或签名冲突。

## 后续

继续等待两个 GSM8K variant 的三个 repeat 完成；完成后马上校验 summary 的 batch16/paper/Kitty-reference 签名和 strict-match，再在释放出的 GPU 上并行安排 MATH-Algebra、GPQA、HumanEval 沙箱及 AIME24/25。Table 3/4 与 Figure 4 未完成前，总复现门禁保持失败，不提前宣称完成。
