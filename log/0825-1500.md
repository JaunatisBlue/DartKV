# 第四轮调整：AIME batch8 + FlashAttention2 正式协议

时间：2026-08-25 15:00（Asia/Shanghai）

Kitty/AIME24 的 batch16 首批在运行 2:04:32 后发生 CUDA OOM：进程已占用约 76.95 GiB，还需分配 2.40 GiB，而当时仅余 2.29 GiB；没有完整 batch，因此未生成 checkpoint，不能计作结果。

论文没有固定准确率 batch size，且论文环境使用 FlashAttention。Table 4 本地正式协议改为 batch8、`flash_attention_2`、max_new_tokens=32768、repeats=1，并启用 expandable CUDA allocator。新进程已启动，初始显存约 19.8 GiB，显著低于旧 batch16；采样参数、模型、Kitty 配置和完整 30 题均不变。

总审计同步要求 Table 4 使用 batch8/FA2，防止旧 OOM 配置或其他签名混入。GPU1 的 Kitty-Pro/MATH 已到 1136/1187，接近完成。
