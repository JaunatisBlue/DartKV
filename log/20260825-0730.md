# 第四轮阶段记录：batch16 精确 checkpoint 实机生效

时间：2026-08-25 07:30（Asia/Shanghai）

## 当前正式任务

- GPU0：Qwen3-8B FP16/GSM8K batch16 repeat0；
- GPU1：Qwen3-8B Kitty-Pro/GSM8K batch16 repeat1（repeat0 已复用）；
- paper sampling、max_new_tokens4096、repeats3。

## 精确断点验证

首个 16-request chunk 已在两条正式任务中原子写盘：

- FP16 `repeat_0_requests.pt`：completed=16、responses=16、batch_size=16、repeat=0；
- Kitty-Pro `repeat_1_requests.pt`：completed=16、responses=16、batch_size=16、repeat=1；
- 两份 checkpoint 均包含 Python/NumPy/Torch RNG 和两张 CUDA device RNG state；
- 写盘后两任务继续下一 chunk，GPU 显存从首批峰值回落到约 46 GiB。

FP16 首批显存峰值约 76.8 GiB，未 OOM；Kitty-Pro 显著更低。后续若进程中断，可从完整
16-request 边界恢复而不改变随机序列。

## 下一步工作安排

1. 继续完成 FP16 repeat0、Kitty-Pro repeat1/2；
2. 完整 repeat 写盘后临时 request checkpoint 自动删除；
3. 更新 Table 3 audit 并进入下一 task 波次。
