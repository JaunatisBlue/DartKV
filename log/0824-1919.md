# 第四轮阶段记录：解除 GPQA gated 数据依赖

时间：2026-08-24 19:19（Asia/Shanghai）

## 上一步概述

上一阶段修正 accuracy batch1 协议，并在两张 A100 上并行运行 GSM8K/Math 的 FP16 与
Kitty-Pro。任务预检发现 GPQA-Diamond 的 Hugging Face 数据集需要授权，本机缓存只有 README。

## 本阶段完成内容

1. 使用 GPQA 作者官方 GitHub 发布的 `dataset.zip`，按作者 README 公布的密码解压到
   `/home/yx/.cache/dartkv/gpqa`；数据不提交到仓库；
2. 新增 `examples/prepare_gpqa.py`，可重复下载并验证：
   - archive SHA-256：`461ae732...08dc`；
   - Diamond CSV SHA-256：`41d1213c...305`；
   - Diamond 行数：198；
3. `reproduce_kitty.py` 对 `gpqa_diamond_cot_n_shot` 自动复用原 lm-eval task config，只把
   `dataset_path` 改成本地官方 CSV；prompt、5-shot、choice shuffle、filter 和 metric 不变；
4. GPQA 路径与 CSV hash 写入每个 experiment signature 和 summary；
5. Qwen3-8B 总验收器要求 GPQA hash 精确匹配，错误数据版本会被拒绝；
6. 本地 task 实例验证为 198 eval docs；全量测试 52 passed。

## 当前运行任务

两张 A100 各运行两个 batch1 Qwen3-8B cell：

- GPU0：FP16/GSM8K、FP16/MATH-Algebra；
- GPU1：Kitty-Pro/GSM8K、Kitty-Pro/MATH-Algebra；
- 每卡显存约 35 GiB、GPU 利用率 99%，没有 OOM。

## 下一步工作安排

1. 继续四条正式 accuracy 三轮任务并按 checkpoint 汇总；
2. 空闲 slot 出现后启动 HumanEval/GPQA 独立 cells；
3. 完成 Table 3 后继续 Figure 4 与 Table 4；
4. 持续运行 Qwen3-8B 总验收器，确保 batch、hash 和 generation length 均正确。
