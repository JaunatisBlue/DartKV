# 第四轮阶段记录：Kitty native operator 身份与源码审计

时间：2026-08-24 18:34（Asia/Shanghai）

## 上一步概述

上一阶段通过 `dart.kitty_kernels` 直接使用 Kitty 的 page allocator、K/V pack、QK/SV Triton
operator，并完成 Qwen3-8B Figure 5 native 曲线与 OOM 边界。本阶段进一步检查是否仍有算子或
Qwen3 模型实现绕开 facade、被包装替换或来自不同源文件。

## 本阶段完成内容

新增 `examples/check_kitty_operator_parity.py` 并生成
`experiments/kitty_operator_audit.json`。审计结果：

1. `qk_kernel`、`sv_kernel`、`kitty_attention_forward`、`quantize_pack_k/v`、
   `KittyCache`、`get_kvcache_kitty`、`KVCache_Layer`、`Qwen3ForCausalLM_Kitty` 共 9 个
   native 符号全部 `same_object=true`；
2. 每个对象的真实源码都位于 `reference/code/Kitty/src/kitty/...`；
3. Qwen3 自定义模型类也改由 `dart.kitty_kernels` facade 暴露，benchmark 不再绕过 facade；
4. page size 128、sink 32、low bit 2、high bit 4、25% channel boost，以及 prefill/decode
   attention 后量化时序全部通过；
5. 对 attention、quant pack、cache、layer、Qwen3 model 五个权威源文件记录 SHA-256；
6. 全量测试 47 passed，`pip check` 仍为 clean。

## 当前实验状态

GPU1 的 Qwen3-8B / GSM8K / Kitty-Pro / paper protocol 三次精度任务持续运行；repeat1 当前
约 1249/1319（95%），显存 41.4 GiB，GPU 利用率约 91%，功耗约 283 W。GPU0 已完成 native
Figure 5，当前空闲。

## 当前判断

算子层没有剩余实现差异：最终 native 系统路径使用的就是 Kitty 权威对象，而非 Dart 的近似
kernel；配置常量与 Qwen3 cache 更新顺序也已由可执行审计证明。剩余工作主要是完成只针对
Qwen3-8B 的精度/消融实验并通过 Table 3/4、Figure 4/5 manifest audit。

## 下一步工作安排

1. 等 GSM8K repeat1 写出 checkpoint，继续自动运行 repeat2，最后计算三次均值；
2. 完成后立即运行 Qwen3-8B Table 3 audit；
3. 继续 Qwen3-8B 的其他 Table 3 任务、Figure 4 random/magnitude sweep 与 Table 4；
4. 每轮继续更新 operator hash、实验 checkpoint 与日志，最终按 manifest 逐项验收。
