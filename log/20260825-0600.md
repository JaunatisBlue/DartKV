# 第四轮阶段记录：精确 stochastic request checkpoint

时间：2026-08-25 06:00（Asia/Shanghai）

## 上一步概述

GSM8K batch1 单轮耗时很长，而完整 repeat 才落盘；若进程故障会丢失数小时。lm-eval 普通
`CachingLM` 对 `do_sample=True` 明确禁用 cache，因此不能直接用于随机采样续跑。

## 本阶段完成内容

新增 `dart.eval_checkpoint.ExactSamplingCheckpointLM`：

1. 每个 batch1 `generate_until` request 完成后原子保存响应；
2. 同时保存 Python、NumPy、Torch CPU 及全部 CUDA RNG 状态；
3. 恢复时复用 lm-eval `Collator` 的真实 grouping/sorting，要求已完成请求是严格前缀；
4. 恢复前设置完整 RNG 状态，只生成剩余 request；
5. 每个 repeat 使用独立 checkpoint，完整 `repeat_N.json` 写盘后自动删除临时文件；
6. signature 不匹配、重复请求、顺序变化或 batch!=1 会直接拒绝；
7. 新增模拟中断测试：第二个 request 后异常，重置所有 RNG 后恢复，输出与无中断基线完全
   一致；全量测试 55 passed。

## 当前运行状态

现有 GSM8K 进程启动于该功能加入前，继续原样运行；后续 MATH/GPQA/HumanEval 波次默认启用
精确 request checkpoint。当前 FP16 约 82/1319、Kitty-Pro 约 65/1319，无 OOM。

## 下一步工作安排

1. 完成现有 GSM8K repeat；
2. 后续 task 使用精确 request checkpoint，降低故障重跑风险；
3. 持续更新 Table 3/4、Figure 4 总验收状态。
