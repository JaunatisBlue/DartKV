# 第四轮阶段记录：GSM8K batch1 单进程持续复现

时间：2026-08-25 01:30（Asia/Shanghai）

## 上一步概述

由于每卡四进程时间片竞争明显，本阶段停止 MATH/GPQA/HumanEval，只保留 GPU0 FP16/GSM8K
与 GPU1 Kitty-Pro/GSM8K，保证单个 sample 获得完整 GPU 并严格保持 batch1 随机协议。

## 当前状态

- FP16：70/1319；
- Kitty-Pro：59/1319；
- 每卡显存约 17.5 GiB；
- GPU 利用率采样约 49%/39%，功耗约 170/124 W；
- 两进程 CPU 100%、无 OOM、无退出，尚无完整 repeat checkpoint。

单样本 CoT 长度差异很大，进度可能数分钟不动后连续完成多个样本。继续使用完整 repeat
原子写盘，避免随机采样 cache 破坏 seed 语义。

## 下一步工作安排

1. 完成 FP16/Kitty-Pro GSM8K 三轮；
2. 读取首轮 summary 并通过 batch1/signature 检查；
3. 依次运行 MATH、GPQA、HumanEval；
4. 保持 Qwen3-8B 唯一模型，最终由总验收器决定完成状态。
