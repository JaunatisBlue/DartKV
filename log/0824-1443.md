# 第四轮阶段记录：完成 Kitty native Figure 5 曲线边界

时间：2026-08-24 14:43（Asia/Shanghai）

## 上一步概述

上一阶段确认 Kitty native operator facade 已接入 benchmark，并完成 Qwen3-8B native
batch32、batch256 端点；本阶段继续补 batch128 和 batch512 OOM，完成原生 Figure 5 的中间点与
最大 batch 边界。

## 本阶段完成内容

固定 Qwen3-8B、单 A100 80GB、FP16、FlashAttention2 prefill、Kitty native facade、paper
protocol（100-token prompt、总长度8192、warmup1/repeat1）：

| batch | generated tok/s | peak allocated | peak reserved |
| ---: | ---: | ---: | ---: |
| 32 | 549.74 | 23.91 GiB | 24.39 GiB |
| 128 | 1349.74 | 46.46 GiB | 47.11 GiB |
| 256 | 1555.43 | 71.25 GiB | 72.08 GiB |

batch512 在 Kitty 静态 KeyCache 分配阶段 OOM；因此最大成功 batch256，较 batch32 提升
8×。batch256/batch32 的 generated throughput 增益为 **2.83×**，落入论文的 2.1--4.1×
区间；batch128 的 1349.74 tok/s 与论文图高端约 1350 tok/s 对齐。所有点通过
`dart.kitty_kernels` 进入 Kitty 原生 `quantize_pack/qk/sv` operator，不是 generic Dart
kernel 诊断结果。

## 当前判断

Kitty 原生代码/算子已经完成搬运入口、8B 端点复现和显存边界验证。系统 Figure 5 的核心
结论（8× batch、同显存 2.83× throughput、batch512 OOM）现在可以作为 native kernel 结果
验收。GPU1 精度任务仍在执行，当前 repeat1 推进到 161/1319。

## 下一步工作安排

1. 从 GPU1 的 GSM8K repeat1/2 checkpoint 继续，完成 Kitty-Pro 三次均值并运行 Table 3 audit；
2. 补 native batch64（可选）以形成更密集曲线，不改变最大 batch/增益验收；
3. 将 native Figure 5 点、OOM 边界和 operator source 写入最终 manifest；
4. 只用 Qwen3-8B 继续 Table 3 其余任务、Figure 4 random/magnitude sweep 和 Table 4；
5. 后续任何系统数字必须注明 native Kitty operator 或 generic Dart reference，避免混淆。
