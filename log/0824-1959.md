# 第四轮阶段记录：HumanEval sandbox 正式启动

时间：2026-08-24 19:59（Asia/Shanghai）

## 上一步概述

上一阶段启动了 GPQA FP16/Kitty-Pro 三轮，并准备 HumanEval bubblewrap；首次 sandbox 预检
因根目录只读导致 `datasets` lock 无法创建。本阶段修正 cache 挂载后重启正式任务。

## 本阶段完成内容

1. HumanEval sandbox 现在同时挂载一个可写、可丢弃的 HumanEval datasets cache 副本；
2. `HF_METRICS_CACHE=/tmp/hf_metrics`，避免 code_eval Arrow lock 写入真实 home cache；
3. `HF_DATASETS_OFFLINE=1`、`HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1`，网络继续由
   bubblewrap namespace 禁用；
4. 离线 task loading smoke 已通过，HumanEval 164 docs/unsafe metric 正常加载；
5. 两条正式 HumanEval 已启动：Qwen3-8B FP16/GPU0、Kitty-Pro/GPU1，batch1/repeats3；
6. 新增 sandbox 行为测试，全量测试 54 passed。

## 当前运行状态

每张 A100 现在有四个进程（GSM8K、MATH、GPQA、HumanEval），显存约 69.2 GiB、利用率 99%，
尚未 OOM。GPQA 与 HumanEval 生成前缀速度受长 CoT 影响较大，但进程均存活。

## 下一步工作安排

1. 等 GPQA/HumanEval 先完成首轮 checkpoint，核对指标与实验签名；
2. 继续 GSM8K/MATH 三轮；
3. 运行 Qwen3-8B 总验收器，更新 Table 3 缺项数量；
4. 空闲后继续 Figure 4、Table 4，所有准确率仍固定 batch1。
