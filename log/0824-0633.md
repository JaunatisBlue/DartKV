# 第四轮阶段记录：Figure 5 Static 对照与 Kitty 中间点

时间：2026-08-24 06:33（Asia/Shanghai）

## 上一步概述

上一阶段接入并验证了 Figure 5 官方 HF Static FP16、Quanto INT4 路径，以及 artifact 使用的
HQQ backend；Dynamic FP16 曲线已有 batch8/16/32，Kitty 已验证 batch8/256 和 batch512 OOM。
本阶段继续补 Static 最大 batch、Kitty 中间 batch，并把散落结果汇总成可审计的双面板图。

## 本阶段完成内容

1. HF Static FP16 batch32 在 8192 长度正式协议下达到 611.78 sequence tok/s、604.31
   generated tok/s，peak reserved 55.64 GB；batch64 在 StaticCache 预分配时 OOM，故最大 batch
   为 32；
2. Kitty batch128 达到 1374.25 sequence tok/s、1357.47 generated tok/s，peak reserved
   47.11 GB，KV storage 28.69 GB、dense equivalent 154.62 GB，压缩比 5.39×；
3. Kitty batch256 相对 Static batch32 的端点吞吐增益为 2.58×，落入论文报告的
   2.1--4.1×；相对 Dynamic batch32 为 5.03×，高于论文区间上限；
4. 新增 `examples/plot_kitty_figure5.py`，自动汇总正式 JSON 并生成显存/吞吐双面板；显存默认
   使用一致的 peak allocated 口径，reserved 模式绝不混入 allocated fallback；
5. 长 benchmark 增加 warmup 完成和 measured repeat 开始提示，后续不再依靠 GPU 利用率猜测
   运行阶段；
6. 更新机器可读 Figure 5 reproduction JSON，全量测试 42 passed。

## 当前判断

Figure 5 的主要结论已在本机形成闭环：Dynamic/Static FP16 最大 batch32，Kitty 最大
batch256，容量提升 8×；对更快的 Static baseline 仍有 2.58× 吞吐提升。当前 Kitty
batch128 的 1357.47 generated tok/s 已接近论文图高端约 1350 tok/s，batch256 则进一步达到
1560.64 tok/s。剩余工作是补曲线低/中 batch 和 HF KIVI INT4 正式长序列点。

## 下一步工作安排

1. GPU0 继续 Kitty batch64、32，形成 8/32/64/128/256 的曲线；
2. 运行 Quanto INT4 的 8192 长序列点与 OOM sweep，对应论文 HF KIVI INT4；
3. 逐步补 Static/Dynamic 的低 batch 点，并持续重绘 Figure 5；
4. GPU1 继续 Kitty-Pro/GSM8K repeat 1、2，三次完成后立即执行 Table 3 audit；
5. Figure 5 曲线完成后把主要算力转到 Table 3、Figure 4 与 Table 4，直到 manifest 无缺项。
