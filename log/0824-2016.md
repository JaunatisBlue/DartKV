# 第四轮阶段记录：单进程波次后的 GSM8K 主任务

时间：2026-08-24 20:16（Asia/Shanghai）

## 上一步概述

八进程并发导致每卡功耗只有约 230/209 W、任务时间片竞争明显，且没有任何完整 checkpoint。
本阶段停止 MATH、GPQA、HumanEval，仅保留 GPU0 FP16/GSM8K 与 GPU1 Kitty-Pro/GSM8K，改为
单卡单进程波次。

## 当前状态

- GPU0/1 各约 17.5 GiB 显存；
- 当前 GPU 利用率采样约 37%/39%，功耗约 147/125 W；
- Kitty-Pro GSM8K 进度约 54/1319；
- FP16 GSM8K 进度约 68/1319；
- 两进程均 CPU 100%、状态正常，无 OOM 或 checkpoint。

单样本长 CoT 导致每条 GSM8K 请求耗时几十秒到数分钟，单进程波次比四路抢占更适合保留
协议与 GPU 稳定性，但完整 repeat 仍需较长时间。

## 下一步工作安排

1. 继续完成 FP16/Kitty-Pro GSM8K 三轮；
2. 首轮写盘后核对 strict-match、seed 和 batch1 signature；
3. 再逐 task 波次运行 MATH、GPQA、HumanEval；
4. 只有 Table 3/4、Figure 4/5 和 operator audit 全部通过才结束目标。
