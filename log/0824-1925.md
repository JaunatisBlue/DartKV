# 第四轮阶段记录：HumanEval 隔离执行准备

时间：2026-08-24 19:25（Asia/Shanghai）

## 上一步概述

上一阶段解除 GPQA gated 数据依赖。HumanEval task 资源预检确认共有 164 条、指标为 pass@1，
但 metric 会执行模型生成的 Python，必须显式确认并隔离。

## 本阶段完成内容

1. `reproduce_kitty.py` 不再因 task 名称自动确认 unsafe code；默认保持拒绝；
2. 只有显式传入 `--confirm-run-unsafe-code` 时才设置 `HF_ALLOW_CODE_EVAL=1`；
3. 新增 `examples/run_humaneval_sandbox.sh`：
   - 使用 bubblewrap；
   - 根文件系统/仓库只读；
   - 仅结果目录可写；
   - `/tmp` 为临时内存文件系统；
   - 禁用网络；
   - 固定 Qwen3-8B、paper、batch1、repeats3、max_new_tokens4096；
4. bubblewrap read-only/network namespace smoke 通过，shell 语法检查通过；
5. 新增默认不确认 unsafe code 的测试，全量测试 53 passed。

## 当前运行任务

GPU0/1 各两个 batch1 accuracy cell，总显存约 35 GiB/卡、利用率 99%，分别运行 FP16 与
Kitty-Pro 的 GSM8K/MATH-Algebra。

## 下一步工作安排

1. 继续当前四条 Table 3 三轮任务；
2. 空闲 GPU 出现后通过 sandbox wrapper 运行 HumanEval；
3. 通过已准备的官方本地 CSV 运行 GPQA；
4. 持续由 Qwen3-8B 总验收器检查协议与完成度。
