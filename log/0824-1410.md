# 第四轮阶段记录：Kitty 原生 kernel facade 的 Figure 5 端点

时间：2026-08-24 14:10（Asia/Shanghai）

## 上一步概述

上一阶段已按要求把 Kitty 的原生 Triton operator 作为 DartKV 的统一入口：
`dart.kitty_kernels` 暴露 `quantize_pack_k/v`、`qk_kernel`、`sv_kernel`、静态
`KVCache_Layer` 和 `KittyCache`，`kitty-engine` benchmark 通过该 facade 调用。此阶段先做
短 8B smoke，再恢复完整 Figure 5 与 GSM8K 精度任务。

## 本阶段完成内容

### 原生 Kitty operator smoke

Qwen3-8B、100-token prompt、max_seq_len256、batch1、warmup1/repeat1：

- Kitty native generated throughput：15.89 tok/s；
- 首 32 output token IDs 与 Dart fused smoke 一致；
- facade import 和 kernel 符号测试通过。

### Figure 5 原生端点

固定 Qwen3-8B/A100/FP16/FlashAttention2/paper protocol/总长度8192：

| batch | generated tok/s | peak allocated | peak reserved |
| ---: | ---: | ---: | ---: |
| 32 | 549.74 | 23.91 GiB | 24.39 GiB |
| 256 | 1555.43 | 71.25 GiB | 72.08 GiB |

batch256 相对 batch32 正好 **8×**，generated throughput 增益 **2.83×**，在论文的
2.1--4.1× 区间内；KV storage 压缩比 5.39×。结果来自 `dart.kitty_kernels` facade，
写入 `experiments/kitty_figure5_reproduction.json` 的
`final_native_kernel_reproduction`，不是早期 generic Dart kernel 诊断点。

### 精度任务

GPU1 已启动 Qwen3-8B / GSM8K / Kitty-Pro / paper protocol / Kitty simulation backend 的
三次断点复现，repeat0 checkpoint 已复用，repeat1 当前推进到 81/1319。该任务只使用 8B，
不会与 GPU0 的系统端点混用。

## 当前判断

用户要求的“直接搬用 Kitty kernel”已经落实到正式系统入口，Figure 5 的 native endpoint 已
复现 8× batch 与论文吞吐区间。Dart 自己的 `dart-engine` 仍保留为实现研发路径，但其短点
速度不用于声称 Kitty 论文性能；系统验收以 native operator 路径为准。

## 下一步工作安排

1. 补 native Kitty batch64/128 中间点和 batch512 OOM，完成 Figure 5 曲线/边界审计；
2. GPU1 继续三次 GSM8K，完成后立即运行 Table 3 audit；
3. 在只用 Qwen3-8B 的前提下，继续 Table 3 其余任务、Figure 4 random/magnitude sweep 和
   Table 4 AIME；
4. 所有后续系统实验都从 `dart.kitty_kernels` native facade 进入，避免 generic kernel 结果
   混入最终 manifest。
