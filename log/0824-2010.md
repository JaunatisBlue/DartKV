# 第四轮阶段记录：Qwen3-8B 全 Table 3 任务并行运行

时间：2026-08-24 20:10（Asia/Shanghai）

## 上一步概述

上一阶段完成 HumanEval sandbox 的 datasets cache 修正，并启动 FP16/Kitty-Pro HumanEval。
同时 GPQA、GSM8K、MATH 的六条 batch1 正式任务继续运行。

## 当前运行队列

两张 A100 各 4 个 Qwen3-8B 进程：

- GPU0：FP16 GSM8K、FP16 MATH、FP16 GPQA、FP16 HumanEval；
- GPU1：Kitty-Pro GSM8K、Kitty-Pro MATH、Kitty-Pro GPQA、Kitty-Pro HumanEval；
- 全部 paper protocol、batch1、repeats3；HumanEval 使用 bubblewrap、离线 datasets、
  临时 metrics cache。

两卡显存约 69.9/70.1 GiB，利用率 99%，功耗约 241/212 W，未发生 OOM。

## 结果状态

当前尚无完整 repeat 文件写出；这是 runner 的原子 checkpoint 语义：每个 repeat 必须完成
所有样本后才写 `repeat_N.json`，避免随机采样中途恢复改变 RNG 状态。进程和 GPU 利用率均
正常，没有证据表明任务卡死。

## 下一步工作安排

1. 继续等待 GPQA/HumanEval 首轮完成；
2. 读取首轮 summary，校验 task metric、batch1、seed、GPQA CSV hash；
3. 完成所有 Table 3 cell 后运行 Qwen3-8B 总验收器；
4. 继续 Figure 4 和 Table 4，直到 manifest gate 全部通过。
