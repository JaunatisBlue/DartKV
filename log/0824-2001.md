# 第四轮阶段记录：GPQA 与 HumanEval 并行执行

时间：2026-08-24 20:01（Asia/Shanghai）

## 上一步概述

上一阶段六条 Table 3 任务继续运行，并修正 HumanEval sandbox 的 datasets/metrics cache 挂载。
本阶段正式启动两种 accuracy variant 的 GPQA-Diamond 与 HumanEval。

## 当前任务队列

每张 A100 四个 Qwen3-8B batch1 进程：

- GPU0：FP16 GSM8K、FP16 MATH、FP16 GPQA、FP16 HumanEval sandbox；
- GPU1：Kitty-Pro GSM8K、Kitty-Pro MATH、Kitty-Pro GPQA、Kitty-Pro HumanEval sandbox；
- 所有任务 paper sampling、repeats3、max_new_tokens4096；
- HumanEval 代码执行仅在 bubblewrap 中进行，网络关闭、仓库只读、临时 cache 可写。

## 进度

- GPQA FP16：5/198；Kitty-Pro：47/198；
- HumanEval FP16：5/164；Kitty-Pro 已进入生成；
- GSM8K/MATH 仍在前几%的长 CoT 样本中；
- 每卡显存约 69.5 GiB，GPU 利用率 99%，未发生 OOM。

## 下一步工作安排

1. 优先等待 GPQA/HumanEval 的首轮完整 checkpoint；
2. 读取并校验 `gpqa_data_sha256`、unsafe sandbox 配置和 pass@1/exact-match metric；
3. 继续 GSM8K/MATH 三轮，完成 Table 3；
4. 运行总验收器后继续 Figure 4/Table 4，所有实验只使用 Qwen3-8B。
