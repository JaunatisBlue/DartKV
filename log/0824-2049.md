# 第四轮阶段记录：切换 A100 accuracy 到 batch16

时间：2026-08-24 20:49（Asia/Shanghai）

## 上一步概述

严格 batch1 GSM8K 单轮预计需要数十小时，难以完成 Qwen3-8B 全矩阵。重新审阅论文与 Kitty
artifact 后确认：论文未固定 accuracy batch size，`accuracy_eval.sh` 将 batch 作为显式参数；
论文要求的是 sampling 配置、3--10 次重复与均值/最大偏差。

## 本阶段完成内容

1. accuracy 正式协议固定为 batch16，适配单张 A100，同时保留 temperature0.6、top-p0.95、
   top-k20、seed bases、repeats3 和 generation length；
2. Qwen3-8B 总验收器改为要求 batch16，拒绝其他 batch 的 summary；
3. exact stochastic checkpoint 扩展为任意固定 batch：只在完整 batch 边界原子写入响应与
   Python/NumPy/Torch/CUDA RNG 状态；中途 batch 不写盘，恢复时要求 chunk-aligned prefix；
4. batch2 第二个 batch 中途故障测试通过，恢复输出与无中断完全一致；
5. Qwen3-8B `limit=1` 真实 lm-eval wrapper smoke 通过，完整 repeat 后临时 checkpoint 删除；
6. 停止 batch1 GSM8K，GPU1 Kitty-Pro 复用此前签名一致的 batch16 repeat0（93.63%），继续
   repeat1/2；GPU0 FP16 从 repeat0 开始；
7. 全量测试 55 passed。

## 当前运行状态

- GPU1 Kitty-Pro/GSM8K batch16：已复用 repeat0，正在 repeat1；
- GPU0 FP16/GSM8K batch16：正在 repeat0；
- 显存约 24.2/25.2 GiB，GPU 利用率约 94%，功耗约 302/291 W；
- request checkpoint 将在首个 16-request chunk 完成后出现。

## 下一步工作安排

1. 完成 GSM8K 三轮并更新 Table 3 audit；
2. 以 batch16 单进程波次运行 MATH、GPQA、sandboxed HumanEval；
3. 后续 Figure 4/Table 4 同样使用记录明确的 A100 batch16（AIME 视显存调整并记录）；
4. 仅在所有 Qwen3-8B gate 通过后宣称完成。
