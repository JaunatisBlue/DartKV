# 第四轮阶段记录：补齐 Figure 5 官方 HF baselines

时间：2026-08-24 05:49（Asia/Shanghai）

## 上一步概述

上一阶段已经在 Qwen3-8B/A100/100-token prompt/总长度 8192 协议下复现 Figure 5 的关键
端点：HF Dynamic FP16 最大 batch32、Kitty 最大 batch256，batch 容量提升 8×，端点吞吐提升
5.03×；两侧翻倍后的 batch64/batch512 均用真实 OOM 验证。本阶段继续补曲线中间点，并把
Kitty artifact 中剩余的 HF cache baseline 接入同一基准。

## 本阶段完成内容

1. 新增 HF Static FP16、Quanto INT4 和 HQQ INT4 cache 入口；sweep 同步支持这些 backend，
   量化结果记录 bit、axis、group size、residual length 和实际 packed storage；
2. Static FP16 已完成真实 A100 CUDA generate smoke；
3. Quanto 首次使用时已成功为 A100 `sm80` 编译 `quanto_cuda` 扩展，随后真实 INT4 cache
   forward/generate 通过；短序列 storage 13.44 MB，相对 dense 37.75 MB 为 2.81×；
4. HQQ INT4 也完成真实 CUDA generate smoke，确认 HQQ 与 Quanto 两个已安装 backend 都不是
   仅能 import；
5. Figure 5 的 Dynamic FP16 batch16 中间点完成：258.92 sequence tok/s，峰值 allocated
   36.00 GB、reserved 77.32 GB；当前 Dynamic 曲线已有 batch8/16/32 三点；
6. 全量测试通过：41 passed。

## 当前判断

当前性能环境已经覆盖 Kitty 官方 latency artifact 的 Kitty、HF Dynamic FP16、HF Static FP16
和 HF Quantized INT4 路径，并额外提供 HQQ backend。DynamicCache 的 reserved 显存显著大于
allocated，后续曲线必须同时报告两者，不能仅用 storage tensor 总量解释 OOM。

## 下一步工作安排

1. 在总长度 8192 上运行 Static FP16 的最大 batch/吞吐点，随后运行 Quanto INT4 sweep；
2. 补 Kitty batch32/64/128 三个 paper-protocol 点，完成 Figure 5 曲线形状；
3. GPU1 继续 GSM8K Kitty-Pro repeat 1、2，完成后以三次均值执行 Table 3 audit；
4. 将所有成功点、OOM 边界、backend 和环境版本持续写入机器可读 reproduction JSON；
5. Figure 5 完整后继续 Table 3、Figure 4 promotion sweep 与 Table 4 AIME，直至 manifest
   不再有缺失项。
